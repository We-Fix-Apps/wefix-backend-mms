// Local enum definitions (replacing GraphQL enums)

export enum CompanyStatus {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
  SUSPENDED = 'SUSPENDED',
}

export enum Actions {
  IN = 'in',
  OUT = 'out',
  BREAK = 'break',
  LEAVE = 'leave',
  SMOKE_BREAK = 'smoke break',
}

